# Leave empty to use Vite dev proxy (recommended for local dev).
# Only set these if you're NOT using the Vite proxy (e.g., production build).
# VITE_API_BASE=http://localhost:8001
# VITE_WS_BASE=ws://localhost:8001

